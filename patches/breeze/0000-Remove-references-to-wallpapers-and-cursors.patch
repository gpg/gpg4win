if uname | grep --quiet 'Darwin'; then
    SED_I="sed -i ''"
else
    SED_I="sed -i"
fi
$SED_I '/add_subdirectory(wallpapers)/d' CMakeLists.txt
$SED_I '/add_subdirectory(cursors)/d' CMakeLists.txt
# Note: In Breeze 6.7 we can use `-DBUILD_CURSOR=OFF` instead of patching out `add_subdirectory(cursors)`
