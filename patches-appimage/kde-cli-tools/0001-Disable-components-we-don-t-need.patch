#! /bin/sh
patch -p1 -l -f $* < $0
exit $?

From 3b4092e73f01d9765437b3d73b085a007d3d3732 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Ingo=20Kl=C3=B6cker?= <dev@ingo-kloecker.de>
Date: Thu, 25 Jun 2026 13:36:49 +0200
Subject: [PATCH] Disable components we don't need

--

We only need kde-open.
---
 CMakeLists.txt | 60 +++++++++++++++++++++++++-------------------------
 1 file changed, 30 insertions(+), 30 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 874df90e..6590099a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -30,7 +30,7 @@ include(KDEGitCommitHooks)

 find_package(Qt${QT_MAJOR_VERSION} ${QT_MIN_VERSION} CONFIG REQUIRED COMPONENTS
     Widgets
-    Svg
+    # Svg
     DBus
 )

@@ -45,22 +45,22 @@ if (NOT Qt${QT_MAJOR_VERSION}Test_FOUND)
 endif()

 find_package(KF5 ${KF5_MIN_VERSION} REQUIRED COMPONENTS
-    Config
-    DocTools
-    IconThemes
+    # Config
+    # DocTools
+    # IconThemes
     I18n
-    KCMUtils
+    # KCMUtils
     KIO
-    Service
-    WindowSystem
-    Activities
-    Parts
+    # Service
+    # WindowSystem
+    # Activities
+    # Parts
     CoreAddons
     OPTIONAL_COMPONENTS
-    Su
+    # Su
     QUIET
 )
-find_package(LibKWorkspace ${PROJECT_VERSION} REQUIRED)
+# find_package(LibKWorkspace ${PROJECT_VERSION} REQUIRED)

 # Disables automatic conversions from QString (or char *) to QUrl.
 add_definitions(-DQT_NO_URL_CAST_FROM_STRING)
@@ -78,8 +78,8 @@ ecm_set_disabled_deprecation_versions(QT 5.15.2)

 include_directories(${CMAKE_CURRENT_BINARY_DIR})

-add_subdirectory(doc)
-kdoctools_install(po)
+# add_subdirectory(doc)
+# kdoctools_install(po)

 function(install_compat_symlink executable_target)
     add_custom_command(TARGET ${executable_target} POST_BUILD
@@ -87,31 +87,31 @@ function(install_compat_symlink executable_target)
     install(FILES ${CMAKE_CURRENT_BINARY_DIR}/${executable_target}5 DESTINATION ${KDE_INSTALL_FULL_BINDIR})
 endfunction()

-add_subdirectory(kcmshell)
-add_subdirectory(keditfiletype)
-add_subdirectory(kmimetypefinder)
-if (QT_MAJOR_VERSION EQUAL "5")
-    add_subdirectory(ktraderclient)
-endif()
+# add_subdirectory(kcmshell)
+# add_subdirectory(keditfiletype)
+# add_subdirectory(kmimetypefinder)
+# if (QT_MAJOR_VERSION EQUAL "5")
+#     add_subdirectory(ktraderclient)
+# endif()
 add_subdirectory(kioclient)
-add_subdirectory(ksvgtopng)
-add_subdirectory(kdeinhibit)
-add_subdirectory(plasma-open-settings)
-add_subdirectory(kinfo)
+# add_subdirectory(ksvgtopng)
+# add_subdirectory(kdeinhibit)
+# add_subdirectory(plasma-open-settings)
+# add_subdirectory(kinfo)

-if(Qt5X11Extras_FOUND)
-    add_subdirectory(kstart)
-endif()
+# if(Qt5X11Extras_FOUND)
+#     add_subdirectory(kstart)
+# endif()


 if(KF5Su_FOUND)
     add_subdirectory(kdesu)
 endif()

-if(UNIX)
-    add_subdirectory(kdeeject)
-    add_subdirectory(kbroadcastnotification)
-endif()
+# if(UNIX)
+#     add_subdirectory(kdeeject)
+#     add_subdirectory(kbroadcastnotification)
+# endif()

 check_include_files(sys/wait.h HAVE_SYS_WAIT_H)

--
2.54.0
